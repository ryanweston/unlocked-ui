import { installComponent } from '../../utils/install'
import PrimaryButton from './PrimaryButton.vue'

export const UPrimaryButton = installComponent(PrimaryButton)
export default UPrimaryButton

export * from './PrimaryButton.vue'