// TODO: Use this composable to return object with tag information
// to help render the tags for linkable components. There's too much repetitive logic
// being used at the moment to format href's and buttons and router links haven't been considered.

export function useRouterLink() {

}
